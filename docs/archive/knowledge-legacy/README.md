# Legacy Knowledge Archive

Archived knowledge surfaces are retained for no-data-loss history, not for
runtime agent selection.

`legacy-packs/` is the pre-cleanup `knowledge/packs/` tree. Active role
behavior now lives under `packages/agents/src/*/context/`, while runtime knowledge
sources, tools, and graph state live under `knowledge/`.
