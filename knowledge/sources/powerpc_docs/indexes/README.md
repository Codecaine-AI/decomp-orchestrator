# PowerPC Documents Indexes

Planned source-local exports:

- `pages.jsonl`
- `instruction_terms.jsonl`
- `abi_terms.jsonl`
- `citations.jsonl`

The existing CSV page index at `data/indexes/powerpc_pdf_pages.csv` is the
current source of searchable text.
