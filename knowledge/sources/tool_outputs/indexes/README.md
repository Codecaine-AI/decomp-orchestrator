# Tool Outputs Indexes

Planned source-local exports:

- `ghidra_xrefs.jsonl`
- `opseq_matches.jsonl`
- `mismatch_patterns.jsonl`
- `mwcc_debug_summaries.jsonl`
- `tool_findings.jsonl`

These indexes should be generated from tool caches and emitted to the shared
graph only when a tool result is reproducible enough to cite.
