# Reference Docs Source

Co-located source for the remaining imported reference docs and legacy skills
that do not belong to a more specific source or tool slice.

Actual data lives in `data/docs` and `data/skills`.
